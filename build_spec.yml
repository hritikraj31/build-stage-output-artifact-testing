version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "minor_version" : "3.0"
  exportedVariables:
    - testEnv

inputArtifacts:
  - name: hello-dev-jar
    location: /workspace/Source1/hello123.class

steps:
  - type: Command
    name: "Build Source"
    timeoutInSeconds: 4000
    command: |
      echo "Testing Output Artifacts Upload from Build Stage"
      echo "this is binary artifact" > binary_artifact1.txt
      sleep 60s

outputArtifacts:
  - name: output1
    type: BINARY
    location: ${OCI_PRIMARY_SOURCE_DIR}/binary_artifact1.txt
    registryId: ocid1.artifactrepository.oc1.phx.0.amaaaaaab6fywtyansanskq2n4g7jdec262mzywc4h4exiernwlgd2rit3ea
    path: target/binary_artifact1
    version: ${minor_version}
