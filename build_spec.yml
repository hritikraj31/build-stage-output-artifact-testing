version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "minor_version" : "3.0"
  exportedVariables:
    - testEnv

inputArtifacts:
  - name: hello-dev-jar
    location: /workspace/Source1/hello123.class

steps:
  - type: Command
    name: "Build Source"
    timeoutInSeconds: 4000
    command: |
      echo "Testing Output Artifacts Upload from Build Stage"
      echo "this is binary artifact" > binary_artifact1.txt
      sleep 60s

outputArtifacts:
  - name: output1
    type: BINARY
    location: ${OCI_PRIMARY_SOURCE_DIR}/binary_artifact1.txt
    registryId: ocid1.artifactrepository.oc1.iad.0.amaaaaaab6fywtyaagyhthdt3ywtgyu5bbc445ai5x5yrwv5czhf6oclnijq
    path: target/binary_artifact1
    version: ${minor_version}
